openapi: 3.0.3
info:
  title: AW3 Platform API
  description: |
    Backend API for AW3 Platform MVP - A Web3-native creator economy platform.
    
    ## Authentication Architecture
    
    **IMPORTANT: Two Separate Authentication Systems**
    
    ### 1. Platform Authentication (Privy)
    - **Purpose:** User login to access AW3 platform
    - **Provider:** Privy
    - **Methods:** Gmail, Twitter (via Privy social login), Wallet (MetaMask, WalletConnect, etc.)
    - **Result:** JWT tokens for AW3 API access
    - **Endpoints:** `/auth/*`
    
    ### 2. Social Verification (Independent Service)
    - **Purpose:** Verify social accounts and fetch metrics for campaigns
    - **Provider:** Independent backend service (NOT Privy)
    - **Platforms:** Twitter, Discord, Telegram, Instagram, YouTube
    - **Process:** Own OAuth 2.0 implementation to each platform
    - **Result:** Verified social accounts with follower counts and engagement metrics
    - **Endpoints:** `/creator/profile/social-verification`, `/social/*`
    
    **Key Distinction:**
    - A user can log into AW3 with Gmail but verify their Twitter account for metrics
    - A user can log into AW3 with Twitter (via Privy) but STILL needs separate Twitter verification for social metrics
    - These are completely independent flows with no dependency between them
    
    ## User Roles
    CREATOR, PROJECTOR, ADMIN, VALIDATOR
    
    ## Key Features
    - Privy-based platform authentication (email/social/wallet)
    - Independent social verification service with OAuth implementation
    - Campaign management with escrow
    - CVPI (Creator Value Performance Index) scoring with integrated reputation
    - Certificate and achievement system
  version: 1.0.0
  contact:
    name: Allweb3 Labs
    url: https://allweb3.com
  license:
    name: MIT

servers:
  - url: https://aw3-platform-mock-api.onrender.com/api
    description: Production Mock API Server
  - url: http://localhost:3000/api
    description: Local Development

tags:
  - name: Auth
    description: |
      AW3 Platform Authentication via Privy
      
      **Important Architecture Note:**
      - **Privy** is used ONLY for AW3 platform authentication (user login to access AW3)
      - Supports Gmail, Twitter social login (via Privy), and Wallet-based authentication
      - Issues JWT tokens for AW3 platform access
      
      **Social Verification is COMPLETELY SEPARATE:**
      - Social verification is handled by an independent backend service
      - NOT part of Privy authentication flow
      - Has its own OAuth implementation to Twitter/Discord/Telegram
      - Fetches social metrics (followers, engagement) directly from platform APIs
      - No dependency between Privy authentication and social verification
  - name: Creator Profile
    description: Creator profile management
  - name: Creator Campaigns
    description: Campaign discovery for creators
  - name: Creator Applications
    description: Application management for creators
  - name: Creator Deliverables
    description: Deliverable submission for creators
  - name: Creator Earnings
    description: Earnings and payments for creators
  - name: Creator CVPI
    description: CVPI score and analytics with reputation data
  - name: Creator Certificates
    description: Certificate and achievement management
  - name: Creator Settings
    description: User settings management
  - name: Creator System
    description: System-level reference data for creators (e.g., localization literals)
  - name: Dashboard
    description: Dashboard data endpoints
  - name: Filters
    description: Filter options for campaign search
  - name: Public Marketplace
    description: Public marketplace endpoints
  - name: Project Dashboard
    description: Project dashboard and statistics
  - name: Project Campaigns
    description: Campaign management for projects
  - name: Project Applications
    description: Application review and management
  - name: Project Deliverables
    description: Deliverable verification and payments
  - name: Project Analytics
    description: Campaign analytics and insights
  - name: Project Finance
    description: Budget and payment management
  - name: Project Creators
    description: Creator discovery and management
  - name: Admin Dashboard
    description: Admin overview and platform-level statistics
  - name: Admin Creators
    description: Admin creator management and reputation controls
  - name: Admin Projects
    description: Admin project management and reputation controls
  - name: Admin Campaigns
    description: Admin campaign oversight and force actions
  - name: Admin Finance
    description: Admin financial oversight, fee and distribution configuration
  - name: Admin CVPI
    description: Admin CVPI algorithm, benchmarks, and analytics
  - name: Admin Disputes
    description: Admin dispute resolution and arbitration
  - name: Admin System
    description: Admin system health, emergency controls, and audit logs
  - name: Admin Search
    description: Admin global search across creators, projects, campaigns, and disputes

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT token obtained from wallet authentication

  schemas:
    # ============ ENUMS ============
    UserRole:
      type: integer
      enum: [1, 2, 3, 4]
      description: |
        User role enum:
        1 = Creator
        2 = Projector
        3 = Admin
        4 = Validator
      example: 1

    WalletType:
      type: integer
      enum: [1, 2, 3, 4]
      description: |
        Wallet type enum:
        1 = MetaMask
        2 = WalletConnect
        3 = Coinbase
        4 = Rainbow
      example: 1

    SocialPlatform:
      type: integer
      enum: [1, 2, 3, 4, 5]
      description: |
        Social platform enum:
        1 = Twitter
        2 = YouTube
        3 = Instagram
        4 = TikTok
        5 = Discord
      example: 1

    FocusArea:
      type: integer
      enum: [1, 2, 3, 4, 5, 6, 7, 8, 9]
      description: |
        Focus area enum:
        1 = DeFi
        2 = NFT
        3 = Gaming
        4 = Infrastructure
        5 = L2
        6 = DAO
        7 = Metaverse
        8 = Trading
        9 = Other
      example: 2

    CampaignStatus:
      type: integer
      enum: [1, 2, 3, 4, 5, 6, 7]
      description: |
        Campaign status enum:
        1 = DRAFT
        2 = PENDING_ESCROW
        3 = ACTIVE
        4 = IN_PROGRESS
        5 = COMPLETED
        6 = CANCELLED
        7 = SUSPENDED
      example: 3

    ApplicationStatus:
      type: integer
      enum: [1, 2, 3, 4]
      description: |
        Application status enum:
        1 = PENDING
        2 = ACCEPTED
        3 = REJECTED
        4 = WITHDRAWN
      example: 1

    DeliverableStatus:
      type: integer
      enum: [1, 2, 3, 4, 5]
      description: |
        Deliverable status enum:
        1 = SUBMITTED
        2 = PENDING_VERIFICATION
        3 = VERIFIED
        4 = REJECTED
        5 = PAID
      example: 3

    DeliverableType:
      type: integer
      enum: [1, 2, 3, 4, 5, 6, 7, 8]
      description: |
        Deliverable type enum (UI-aligned):
        1 = Twitter Posts
        2 = Videos
        3 = Articles
        4 = AMAs
        5 = Discord Management
        6 = Community Giveaways
        7 = Instagram Post
        8 = TikTok
      example: 2

    CampaignDuration:
      type: integer
      enum: [1, 2, 3, 4, 5]
      description: |
        Campaign duration enum:
        1 = Less Than 1 Week
        2 = 1-2 Weeks
        3 = 2-4 Weeks
        4 = 1-3 Months
        5 = 3+ Months
      example: 1

    CampaignStage:
      type: integer
      enum: [1, 2, 3, 4, 5, 6]
      description: |
        Campaign stage enum (UI-aligned):
        1 = Ideation
        2 = Live Configuration
        3 = Notification Preferences
        4 = Privacy Controls
        5 = Wallet & Payout
        6 = Security
      example: 2

    Complexity:
      type: integer
      enum: [1, 2, 3]
      description: |
        Complexity enum:
        1 = LOW
        2 = MEDIUM
        3 = HIGH
      example: 2

    PaymentToken:
      type: integer
      enum: [1, 2, 3, 4]
      description: |
        Payment token enum:
        1 = USDC
        2 = USDT
        3 = ETH
        4 = AW3
      example: 1

    TimePeriod:
      type: integer
      enum: [1, 2, 3, 4]
      description: |
        Time period enum:
        1 = 7d
        2 = 30d
        3 = 90d
        4 = 1y
      example: 2

    EarningsRange:
      type: integer
      enum: [1, 2, 3]
      description: |
        Earnings range enum:
        1 = Hourly
        2 = Daily
        3 = Monthly
      example: 3

    ProjectCampaignSort:
      type: integer
      enum: [1, 2, 3, 4, 5]
      description: |
        Sort options for project campaign list:
        1 = Newest
        2 = Budget High to Low
        3 = Applications Most to Least
        4 = CVPI Best to Worst
        5 = End Date Soonest
      example: 1

    ApplicationSort:
      type: integer
      enum: [1, 2, 3, 4, 5]
      description: |
        Application sort enum:
        1 = Newest
        2 = Highest Reputation
        3 = Best CVPI
        4 = Most Followers
        5 = Match Score
      example: 2

    PaymentStructure:
      type: integer
      enum: [1, 2, 3]
      description: |
        Payment structure enum:
        1 = Fixed Payment
        2 = Performance-Based
        3 = Milestone-Based
      example: 1

    ApprovalMethod:
      type: integer
      enum: [1, 2]
      description: |
        Approval method enum:
        1 = Pre-approval review before publishing
        2 = Post-publishing review after publishing
      example: 1

    DurationType:
      type: integer
      enum: [1, 2]
      description: |
        Campaign duration type:
        1 = Fixed Duration with end date
        2 = Ongoing until closed
      example: 1

    # ============ COMMON SCHEMAS ============
    ApiResponse:
      type: object
      required:
        - success
        - timestamp
      properties:
        success:
          type: boolean
        data:
          type: object
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
        timestamp:
          type: string
          format: date-time

    Pagination:
      type: object
      required:
        - currentPage
        - totalPages
        - totalElements
        - pageSize
      properties:
        currentPage:
          type: integer
          example: 0
        totalPages:
          type: integer
          example: 5
        totalElements:
          type: integer
          example: 100
        pageSize:
          type: integer
          example: 20

    # ============ AUTH SCHEMAS ============
    WalletConnectRequest:
      type: object
      required:
        - walletAddress
      properties:
        walletAddress:
          type: string
          example: "0x742d35Cc6634C0532925a3b844Bc454e4438f44e"
        chainId:
          type: string
          example: "1"
        walletType:
          $ref: '#/components/schemas/WalletType'

    WalletConnectResponse:
      type: object
      required:
        - walletAddress
        - nonce
        - message
        - expiresAt
      properties:
        walletAddress:
          type: string
        nonce:
          type: string
        message:
          type: string
          description: Message to sign with wallet
        expiresAt:
          type: integer
          format: int64

    VerifySignatureRequest:
      type: object
      required:
        - walletAddress
        - signature
        - nonce
      properties:
        walletAddress:
          type: string
        signature:
          type: string
        nonce:
          type: string

    RegistrationRequest:
      type: object
      required:
        - walletAddress
        - signature
        - nonce
        - role
        - termsAccepted
      properties:
        walletAddress:
          type: string
          example: "0x742d35Cc6634C0532925a3b844Bc454e4438f44e"
        signature:
          type: string
        nonce:
          type: string
        role:
          $ref: '#/components/schemas/UserRole'
        termsAccepted:
          type: boolean
          example: true

    RegistrationResponse:
      type: object
      required:
        - userId
        - walletAddress
        - role
        - accessToken
        - refreshToken
        - expiresIn
        - profileComplete
      properties:
        userId:
          type: string
          format: uuid
        walletAddress:
          type: string
        role:
          $ref: '#/components/schemas/UserRole'
        accessToken:
          type: string
        refreshToken:
          type: string
        expiresIn:
          type: integer
        profileComplete:
          type: boolean
        nextSteps:
          type: array
          items:
            type: string
          example: ["Complete profile", "Verify social accounts", "Browse campaigns"]

    AuthResponse:
      type: object
      required:
        - accessToken
        - refreshToken
        - tokenType
        - expiresIn
        - user
      properties:
        accessToken:
          type: string
        refreshToken:
          type: string
        tokenType:
          type: string
          example: Bearer
        expiresIn:
          type: integer
          format: int64
        user:
          $ref: '#/components/schemas/UserInfo'

    UserInfo:
      type: object
      required:
        - userId
        - walletAddress
        - role
        - profileComplete
      properties:
        userId:
          type: string
          format: uuid
        walletAddress:
          type: string
        role:
          $ref: '#/components/schemas/UserRole'
        displayName:
          type: string
        profileComplete:
          type: boolean
        subscriptionTier:
          type: string

    # ============ PROFILE SCHEMAS ============
    SocialAccount:
      type: object
      required:
        - platform
        - handle
        - verified
      properties:
        platform:
          $ref: '#/components/schemas/SocialPlatform'
        handle:
          type: string
          example: "@cryptoinfluencer"
        link:
          type: string
          format: uri
          example: "https://twitter.com/cryptoinfluencer"
        followers:
          type: integer
          example: 125000
        verified:
          type: boolean
          example: true
        verifiedAt:
          type: string
          format: date-time

    CreatorProfile:
      type: object
      required:
        - userId
        - walletAddress
        - role
        - profileComplete
      properties:
        userId:
          type: string
          format: uuid
        walletAddress:
          type: string
        email:
          type: string
          format: email
        displayName:
          type: string
        avatar:
          type: string
          format: uri
        bio:
          type: string
        focusArea:
          type: array
          items:
            $ref: '#/components/schemas/FocusArea'
          description: Array of focus areas
        socialAccounts:
          type: array
          items:
            $ref: '#/components/schemas/SocialAccount'
        profileComplete:
          type: boolean
        createdAt:
          type: string
          format: date-time

    CreatorProfileUpdateRequest:
      type: object
      properties:
        displayName:
          type: string
          maxLength: 100
        avatar:
          type: string
          format: uri
        bio:
          type: string
          maxLength: 500
        focusArea:
          type: array
          items:
            $ref: '#/components/schemas/FocusArea'

    # ============ CAMPAIGN SCHEMAS ============
    CampaignListItem:
      type: object
      required:
        - campaignId
        - projectId
        - title
        - status
        - budgetAmount
        - budgetToken
        - deadline
      properties:
        campaignId:
          type: string
          format: uuid
        projectId:
          type: string
          format: uuid
        title:
          type: string
        status:
          $ref: '#/components/schemas/CampaignStatus'
        budgetAmount:
          type: number
          format: decimal
        budgetToken:
          $ref: '#/components/schemas/PaymentToken'
        numberOfApplicants:
          type: integer
          description: Total number of applicants
        numberOfDeliveries:
          type: integer
          description: Number of deliverables required
        deadline:
          type: string
          format: date-time
        kpiTargets:
          type: object
        requiredReputation:
          type: integer
        projectAvatar:
          type: string
          format: uri
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CampaignDetail:
      type: object
      required:
        - campaignId
        - projectId
        - title
        - description
        - status
        - budgetAmount
        - budgetToken
      properties:
        campaignId:
          type: string
          format: uuid
        projectId:
          type: string
          format: uuid
        title:
          type: string
        description:
          type: string
        objective:
          type: string
          description: Explicit campaign objective description
        status:
          $ref: '#/components/schemas/CampaignStatus'
        budgetAmount:
          type: number
        budgetToken:
          $ref: '#/components/schemas/PaymentToken'
        numberOfCreators:
          type: integer
        numberOfApplicants:
          type: integer
        numberOfDeliveries:
          type: integer
        deadline:
          type: string
          format: date-time
        complexity:
          $ref: '#/components/schemas/Complexity'
        kpiTargets:
          type: object
        requiredReputation:
          type: integer
        projectInfo:
          type: object
          required:
            - projectId
            - projectName
            - projectAvatar
          properties:
            projectId:
              type: string
              format: uuid
            projectName:
              type: string
            projectAvatar:
              type: string
              format: uri
            website:
              type: string
              format: uri
            socialChannels:
              type: array
              items:
                $ref: '#/components/schemas/SocialAccount'
        paymentTerms:
          type: object
          required:
            - paymentMethod
            - paymentSchedule
            - paymentConditions
          properties:
            paymentMethod:
              type: string
              description: Payment method description
            paymentSchedule:
              type: string
              description: Payment schedule (e.g., milestone-based, completion-based)
            paymentConditions:
              type: string
              description: Conditions for payment release
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ============ APPLICATION SCHEMAS ============
    Application:
      type: object
      required:
        - applicationId
        - campaignId
        - creatorId
        - status
      properties:
        applicationId:
          type: string
          format: uuid
        campaignId:
          type: string
          format: uuid
        campaignTitle:
          type: string
        creatorId:
          type: string
          format: uuid
        proposedRate:
          type: number
        proposal:
          type: string
        status:
          $ref: '#/components/schemas/ApplicationStatus'
        portfolioLinks:
          type: array
          items:
            type: string
            format: uri
        relevantExperience:
          type: string
        estimatedCompletionDays:
          type: integer
        matchScore:
          type: number
          nullable: true
        appliedAt:
          type: string
          format: date-time
        reviewedAt:
          type: string
          format: date-time
          nullable: true

    ApplicationRequest:
      type: object
      required:
        - campaignId
        - proposal
      properties:
        campaignId:
          type: string
          format: uuid
        proposedRate:
          type: number
        proposal:
          type: string
          maxLength: 2000
        portfolioLinks:
          type: array
          items:
            type: string
            format: uri
        relevantExperience:
          type: string
        estimatedCompletionDays:
          type: integer

    # ============ DELIVERABLE SCHEMAS ============
    Deliverable:
      type: object
      required:
        - deliverableId
        - campaignId
        - creatorId
        - deliverableType
        - status
      properties:
        deliverableId:
          type: string
          format: uuid
        campaignId:
          type: string
          format: uuid
        creatorId:
          type: string
          format: uuid
        contentUrl:
          type: string
          format: uri
        deliverableType:
          $ref: '#/components/schemas/DeliverableType'
        platform:
          $ref: '#/components/schemas/SocialPlatform'
        status:
          $ref: '#/components/schemas/DeliverableStatus'
        metrics:
          type: object
          properties:
            views:
              type: integer
            likes:
              type: integer
            comments:
              type: integer
            shares:
              type: integer
            engagementRate:
              type: number
        cvpiScore:
          type: number
          nullable: true
        paymentAmount:
          type: number
          nullable: true
        submittedAt:
          type: string
          format: date-time
        verifiedAt:
          type: string
          format: date-time
          nullable: true

    DeliverableRequest:
      type: object
      required:
        - campaignId
        - contentUrl
        - deliverableType
        - platform
      properties:
        campaignId:
          type: string
          format: uuid
        contentUrl:
          type: string
          format: uri
        deliverableType:
          $ref: '#/components/schemas/DeliverableType'
        platform:
          $ref: '#/components/schemas/SocialPlatform'
        description:
          type: string

    # ============ EARNINGS SCHEMAS ============
    EarningsSummary:
      type: object
      required:
        - totalEarned
        - pendingPayments
        - availableBalance
        - averageROI
        - growthRate
      properties:
        totalEarned:
          type: number
          format: decimal
          description: Total earnings to date
        pendingPayments:
          type: number
          format: decimal
          description: Payments pending verification
        availableBalance:
          type: number
          format: decimal
          description: Available for withdrawal
        currency:
          type: string
          example: USDC
        averageROI:
          type: number
          format: decimal
          description: Average return on investment percentage
          example: 45.8
        growthRate:
          type: number
          format: decimal
          description: Month-over-month growth rate percentage
          example: 12.5

    EarningsHistoryChart:
      type: object
      required:
        - data
        - range
      properties:
        data:
          type: array
          items:
            type: object
            required:
              - timestamp
              - amount
            properties:
              timestamp:
                type: string
                format: date-time
              amount:
                type: number
              transactionCount:
                type: integer
        range:
          $ref: '#/components/schemas/EarningsRange'
        totalAmount:
          type: number
        periodStart:
          type: string
          format: date-time
        periodEnd:
          type: string
          format: date-time

    # ============ CVPI SCHEMAS ============
    CVPIScore:
      type: object
      required:
        - userId
        - overallScore
        - components
        - reputation
        - lastUpdated
      properties:
        userId:
          type: string
          format: uuid
        overallScore:
          type: number
          minimum: 0
          maximum: 100
          example: 82.3
        components:
          type: object
          required:
            - engagement
            - reach
            - conversion
            - consistency
            - quality
          properties:
            engagement:
              type: number
            reach:
              type: number
            conversion:
              type: number
            consistency:
              type: number
            quality:
              type: number
        reputation:
          type: object
          required:
            - score
            - tier
            - totalReviews
            - averageRating
          properties:
            score:
              type: number
              example: 87.5
            tier:
              type: string
              example: "GOLD"
            totalReviews:
              type: integer
              example: 42
            averageRating:
              type: number
              example: 4.7
        trend:
          type: string
          enum: [UP, DOWN, STABLE]
        percentile:
          type: integer
          example: 87
        lastUpdated:
          type: string
          format: date-time

    CVPIHistory:
      type: object
      required:
        - history
        - period
        - limit
      properties:
        history:
          type: array
          items:
            type: object
            required:
              - date
              - score
            properties:
              date:
                type: string
                format: date
              score:
                type: number
              reputation:
                type: number
                description: Reputation score at that date
        period:
          $ref: '#/components/schemas/TimePeriod'
        limit:
          type: integer

    # ============ CERTIFICATE SCHEMAS ============
    Certificate:
      type: object
      required:
        - certificateId
        - certificateType
        - issueDate
        - imageUrl
      properties:
        certificateId:
          type: string
          format: uuid
        certificateType:
          type: string
          example: "Top Performer"
          description: Type of certificate
        title:
          type: string
          example: "DeFi Campaign Excellence"
        issueDate:
          type: string
          format: date
        imageUrl:
          type: string
          format: uri
          description: Certificate image URL
        relatedCampaign:
          type: object
          properties:
            campaignId:
              type: string
              format: uuid
            campaignTitle:
              type: string
            projectName:
              type: string
        metadata:
          type: object
          description: Additional certificate metadata

    # ============ SETTINGS SCHEMAS ============
    LanguageSettings:
      type: object
      required:
        - language
      properties:
        language:
          type: string
          example: "en"
          description: Language code (en, zh, es, etc.)
        timezone:
          type: string
          example: "UTC"

    RateConfiguration:
      type: object
      required:
        - hourlyRate
        - currency
        - selected
      properties:
        hourlyRate:
          type: number
          description: Default hourly rate
        dailyRate:
          type: number
        projectRate:
          type: number
        currency:
          type: string
          example: "USDC"
        minimumBudget:
          type: number
        selected:
          type: string
          enum: [hourly, daily, project]
          description: Selected rate mode

    NotificationSettings:
      type: object
      description: Notification preferences for email and push notifications
      required:
        - email
        - push
        - frequency
      properties:
        email:
          type: object
          description: Email notification preferences
          required:
            - newCampaignMatches
            - applicationStatusUpdates
            - paymentReceived
            - messagesFromClients
            - communityInteractions
            - platformAnnouncements
          properties:
            newCampaignMatches:
              type: boolean
              description: Notify when new campaigns match your profile
              example: true
            applicationStatusUpdates:
              type: boolean
              description: Notify when application status changes
              example: false
            paymentReceived:
              type: boolean
              description: Notify when payment is received
              example: true
            messagesFromClients:
              type: boolean
              description: Notify when receiving messages from project owners
              example: true
            communityInteractions:
              type: boolean
              description: Notify about community interactions and mentions
              example: false
            platformAnnouncements:
              type: boolean
              description: Notify about platform updates and announcements
              example: false
        push:
          type: object
          description: Push notification preferences
          required:
            - realTimeMessages
            - campaignDeadlines
            - paymentConfirmations
            - milestoneReminders
          properties:
            realTimeMessages:
              type: boolean
              description: Push notifications for real-time messages
              example: true
            campaignDeadlines:
              type: boolean
              description: Push notifications for upcoming campaign deadlines
              example: true
            paymentConfirmations:
              type: boolean
              description: Push notifications for payment confirmations
              example: false
            milestoneReminders:
              type: boolean
              description: Push notifications for milestone reminders
              example: false
        frequency:
          type: string
          enum: [instant, daily_digest, weekly_summary]
          description: |
            Notification frequency preference:
            - instant: Receive notifications immediately as they occur
            - daily_digest: Receive a daily summary of notifications
            - weekly_summary: Receive a weekly summary of notifications
          example: instant

    PrivacySettings:
      type: object
      description: Privacy settings for profile visibility and information sharing
      required:
        - profileVisibility
      properties:
        profileVisibility:
          type: string
          enum: [PUBLIC, PRIVATE, CONNECTIONS_ONLY]
          description: |
            Profile visibility level:
            - PUBLIC: Profile visible to everyone
            - PRIVATE: Profile only visible to you
            - CONNECTIONS_ONLY: Profile visible only to connected users
          example: PUBLIC
        showEarnings:
          type: boolean
          description: Display total earnings on public profile
          example: false
          default: false
        showCompletedCampaigns:
          type: boolean
          description: Display number of completed campaigns on public profile
          example: true
          default: true
        showSocialAccounts:
          type: boolean
          description: Display verified social accounts on public profile
          example: true
          default: true

    SecuritySettings:
      type: object
      description: |
        **DEPRECATED: Security settings are managed by Privy.**
        
        Wallet and security features including 2FA, login alerts, and trusted devices
        are handled by Privy's authentication system.
      deprecated: true
      properties:
        message:
          type: string
          description: Information message about Privy management
          example: Security settings are managed by Privy. Please use Privy dashboard for security configuration.
        twoFactorEnabled:
          type: boolean
          description: Two-factor authentication status (managed by Privy)
          deprecated: true
        loginAlerts:
          type: boolean
          description: Login alert notifications (managed by Privy)
          deprecated: true
        trustedDevices:
          type: array
          description: List of trusted devices (managed by Privy)
          deprecated: true
          items:
            type: object
            properties:
              deviceId:
                type: string
              deviceName:
                type: string
              lastUsed:
                type: string
                format: date-time

    # ============ DASHBOARD SCHEMAS ============
    DashboardTrending:
      type: object
      required:
        - campaigns
      properties:
        campaigns:
          type: array
          items:
            $ref: '#/components/schemas/CampaignListItem'
        period:
          type: string
          example: "24h"

    DashboardLive:
      type: object
      required:
        - campaigns
      properties:
        campaigns:
          type: array
          items:
            $ref: '#/components/schemas/CampaignListItem'
        activeCount:
          type: integer

    DashboardActionItems:
      type: object
      required:
        - items
      properties:
        items:
          type: array
          items:
            type: object
            required:
              - id
              - type
              - title
              - priority
            properties:
              id:
                type: string
                format: uuid
              type:
                type: string
                enum: [APPLICATION_REVIEW, DELIVERABLE_SUBMIT, PAYMENT_PENDING, PROFILE_INCOMPLETE]
              title:
                type: string
              description:
                type: string
              priority:
                type: string
                enum: [HIGH, MEDIUM, LOW]
              deadline:
                type: string
                format: date-time
              relatedEntityId:
                type: string
                format: uuid

    FooterAnalytics:
      type: object
      required:
        - totalCampaigns
        - activeCampaigns
        - totalCreators
        - totalValueProcessed
      properties:
        totalCampaigns:
          type: integer
          description: Total campaigns on platform
        activeCampaigns:
          type: integer
          description: Currently active campaigns
        totalCreators:
          type: integer
          description: Total registered creators
        totalValueProcessed:
          type: number
          description: Total value processed in USD

    # ============ FILTER SCHEMAS ============
    FilterOptions:
      type: object
      required:
        - focusAreas
        - deliverableTypes
        - durations
        - stages
        - budgetRanges
      properties:
        focusAreas:
          type: array
          items:
            type: object
            properties:
              id:
                $ref: '#/components/schemas/FocusArea'
              name:
                type: string
              count:
                type: integer
        deliverableTypes:
          type: array
          items:
            type: object
            properties:
              id:
                $ref: '#/components/schemas/DeliverableType'
              name:
                type: string
              count:
                type: integer
        durations:
          type: array
          items:
            type: object
            properties:
              id:
                $ref: '#/components/schemas/CampaignDuration'
              name:
                type: string
              count:
                type: integer
        stages:
          type: array
          items:
            type: object
            properties:
              id:
                $ref: '#/components/schemas/CampaignStage'
              name:
                type: string
        budgetRanges:
          type: array
          items:
            type: object
            properties:
              min:
                type: number
              max:
                type: number
              label:
                type: string
              count:
                type: integer

    # ============ PROJECT PORTAL SCHEMAS ============
    ProjectDashboardStats:
      type: object
      required:
        - activeCampaigns
        - pendingApplications
        - budgetAvailable
        - deliverablesSubmitted
      properties:
        activeCampaigns:
          type: integer
          example: 3
        pendingApplications:
          type: integer
          example: 12
        budgetAvailable:
          type: number
          example: 25000
        deliverablesSubmitted:
          type: integer
          example: 3
        totalCampaigns:
          type: integer
          example: 28
        totalSpent:
          type: number
          example: 245000
        reputationScore:
          type: integer
          example: 780
        reputationTier:
          type: string
          example: "B"
        avgCampaignCVPI:
          type: number
          example: 95.3

    CampaignCreateRequest:
      type: object
      required:
        - name
        - description
        - durationType
        - startDate
        - creatorBudget
        - numberOfCreators
        - paymentStructure
        - deliverableTypes
        - contentGuidelines
        - approvalMethod
        - kpiTargets
      properties:
        name:
          type: string
          maxLength: 100
          example: "DeFi Protocol Launch"
        description:
          type: string
          maxLength: 2000
        durationType:
          $ref: '#/components/schemas/DurationType'
        startDate:
          type: string
          format: date
        endDate:
          type: string
          format: date
          nullable: true
        coverImage:
          type: string
          format: uri
        creatorBudget:
          type: number
          minimum: 500
          example: 10000
        numberOfCreators:
          type: integer
          minimum: 1
          maximum: 50
          example: 3
        paymentStructure:
          $ref: '#/components/schemas/PaymentStructure'
        payWithAW3Token:
          type: boolean
          default: false
        deliverableTypes:
          type: array
          items:
            $ref: '#/components/schemas/DeliverableType'
        contentGuidelines:
          type: string
          maxLength: 2000
        approvalMethod:
          $ref: '#/components/schemas/ApprovalMethod'
        kpiTargets:
          type: array
          items:
            type: object
            required:
              - metricType
              - targetValue
              - weight
            properties:
              metricType:
                type: string
                enum: [EngagementRate, Impressions, ClickThroughRate, Conversions, FollowerGrowth, Custom]
              targetValue:
                type: number
              weight:
                type: integer
                minimum: 0
                maximum: 100
              verificationMethod:
                type: string
                enum: [Oracle, Manual, Hybrid]
                default: Oracle
        minReputationScore:
          type: integer
          minimum: 0
          maximum: 1000
          default: 600
        cvpiRange:
          type: object
          properties:
            min:
              type: number
              default: 0
            max:
              type: number
              default: 150

    CampaignTransactionResponse:
      type: object
      required:
        - campaignId
        - transactionHash
        - status
      properties:
        campaignId:
          type: string
          format: uuid
        transactionHash:
          type: string
          example: "0x8a2f3d1c..."
        status:
          type: string
          enum: [PENDING, CONFIRMED, FAILED]
        estimatedConfirmationTime:
          type: integer
        totalLocked:
          type: number

    ProjectCampaignListItem:
      type: object
      required:
        - campaignId
        - name
        - status
        - budgetTotal
        - budgetRemaining
      properties:
        campaignId:
          type: string
          format: uuid
        name:
          type: string
        status:
          $ref: '#/components/schemas/CampaignStatus'
        budgetTotal:
          type: number
        budgetRemaining:
          type: number
        applicationCount:
          type: integer
        approvedCount:
          type: integer
        deliverableCount:
          type: integer
        daysRemaining:
          type: integer
          nullable: true
        cvpiScore:
          type: number
          nullable: true
        cvpiClassification:
          type: string
          enum: [Excellent, Good, Average, Below Average, Poor]
        progressStage:
          type: string
          enum: [Applications, InProgress, Verification, Completed]
        createdAt:
          type: string
          format: date-time
        endDate:
          type: string
          format: date-time
          nullable: true

    CampaignMetrics:
      type: object
      required:
        - budgetStatus
        - applications
        - progress
        - cvpiScore
      properties:
        budgetStatus:
          type: object
          properties:
            total:
              type: number
            remaining:
              type: number
            spent:
              type: number
            percentageUsed:
              type: number
        applications:
          type: object
          properties:
            received:
              type: integer
            approved:
              type: integer
            pendingReview:
              type: integer
            rejected:
              type: integer
        progress:
          type: object
          properties:
            status:
              $ref: '#/components/schemas/CampaignStatus'
            daysElapsed:
              type: integer
            daysTotal:
              type: integer
            percentageComplete:
              type: number
            endDate:
              type: string
              format: date-time
        cvpiScore:
          type: object
          properties:
            current:
              type: number
            classification:
              type: string
            trend:
              type: number

    ProjectApplication:
      type: object
      required:
        - applicationId
        - campaignId
        - creatorId
        - status
      properties:
        applicationId:
          type: string
          format: uuid
        campaignId:
          type: string
          format: uuid
        campaignName:
          type: string
        creatorId:
          type: string
          format: uuid
        creatorName:
          type: string
        creatorAvatar:
          type: string
          format: uri
        reputation:
          type: integer
        reputationTier:
          type: string
        avgCVPI:
          type: number
        cvpiClassification:
          type: string
        socialStats:
          type: array
          items:
            type: object
            properties:
              platform:
                $ref: '#/components/schemas/SocialPlatform'
              followers:
                type: integer
        campaignsCompleted:
          type: integer
        successRate:
          type: number
        applicationMessage:
          type: string
        portfolioLinks:
          type: array
          items:
            type: object
            properties:
              url:
                type: string
                format: uri
              title:
                type: string
              description:
                type: string
        appliedAt:
          type: string
          format: date-time
        status:
          $ref: '#/components/schemas/ApplicationStatus'
        matchScore:
          type: number
          nullable: true

    BatchApprovalRequest:
      type: object
      required:
        - applicationIds
      properties:
        applicationIds:
          type: array
          items:
            type: string
            format: uuid
          minItems: 1

    BatchApprovalResponse:
      type: object
      required:
        - successCount
        - failedIds
      properties:
        successCount:
          type: integer
        failedIds:
          type: array
          items:
            type: string
            format: uuid
        errors:
          type: array
          items:
            type: object
            properties:
              applicationId:
                type: string
                format: uuid
              reason:
                type: string

    CampaignAnalytics:
      type: object
      required:
        - summary
        - cvpiBreakdown
        - kpiAchievement
        - audienceReach
        - costEfficiency
      properties:
        summary:
          type: object
          properties:
            totalSpend:
              type: number
            avgCVPI:
              type: number
            kpiSuccessRate:
              type: number
            avgCreatorReputation:
              type: integer
        cvpiBreakdown:
          type: array
          items:
            type: object
            properties:
              creatorName:
                type: string
              cvpiScore:
                type: number
              classification:
                type: string
        kpiAchievement:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
                format: date
              kpiName:
                type: string
              achievementPercentage:
                type: number
        audienceReach:
          type: object
          properties:
            totalImpressions:
              type: integer
            uniqueReach:
              type: integer
            totalEngagements:
              type: integer
            avgEngagementRate:
              type: number
        costEfficiency:
          type: object
          properties:
            costPer1KImpressions:
              type: number
            costPerEngagement:
              type: number
            costPerConversion:
              type: number

    CampaignFinancials:
      type: object
      required:
        - escrowOverview
        - budgetAllocation
        - paymentHistory
        - feeBreakdown
      properties:
        escrowOverview:
          type: object
          properties:
            smartContractAddress:
              type: string
            chain:
              type: string
              example: "BASE Mainnet"
            totalLocked:
              type: number
            totalReleased:
              type: number
            remainingBalance:
              type: number
            expectedRefund:
              type: number
        budgetAllocation:
          type: object
          properties:
            creatorPayments:
              type: number
            serviceFees:
              type: number
            oracleFees:
              type: number
            escrowBuffer:
              type: number
            unused:
              type: number
        paymentHistory:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
                format: date-time
              type:
                type: string
                enum: [CreatorPayment, ServiceFee, OracleFee, EscrowLock, BufferRefund]
              recipient:
                type: string
              amount:
                type: number
              status:
                type: string
                enum: [Completed, Pending, Failed]
              transactionHash:
                type: string
        feeBreakdown:
          type: object
          properties:
            baseRate:
              type: number
            complexityMultiplier:
              type: number
            reputationDiscount:
              type: number
            tokenPaymentDiscount:
              type: number
            effectiveRate:
              type: number
            totalSaved:
              type: number

    CreatorDiscoveryResult:
      type: object
      required:
        - creatorId
        - displayName
        - reputation
        - avgCVPI
      properties:
        creatorId:
          type: string
          format: uuid
        displayName:
          type: string
        avatar:
          type: string
          format: uri
        reputation:
          type: integer
        reputationTier:
          type: string
        avgCVPI:
          type: number
        cvpiClassification:
          type: string
        socialStats:
          type: array
          items:
            type: object
            properties:
              platform:
                $ref: '#/components/schemas/SocialPlatform'
              followers:
                type: integer
        campaignsCompleted:
          type: integer
        successRate:
          type: number
        verticalExperience:
          type: array
          items:
            $ref: '#/components/schemas/FocusArea'
        estimatedCVPI:
          type: number
        available:
          type: boolean

    # ============ ADMIN ENUMS ============
    AdminUserStatus:
      type: string
      enum: [active, suspended, banned]
    DisputeStatus:
      type: string
      enum: [pending, inArbitration, escalated, resolved]
    DisputeIssueType:
      type: string
      enum: [payment, deliverable, kpiVerification, other]
    DisputeResolutionOutcome:
      type: string
      enum: [favorCreator, favorProject, split]
    AdminForceAction:
      type: string
      enum: [forceApproveApplication, forceVerifyDeliverable, forceCancelCampaign, refundEscrow, releasePayment]

    # ============ ADMIN SCHEMAS ============
    AdminDashboardStats:
      type: object
      properties:
        totalCreators:
          type: integer
        totalProjects:
          type: integer
        totalCampaigns:
          type: integer
        activeDisputes:
          type: integer
        platformRevenue30d:
          type: number
          format: double
        treasuryBalance:
          type: number
          format: double
        systemStatus:
          $ref: '#/components/schemas/AdminSystemHealth'
        recentActivity:
          type: array
          items:
            $ref: '#/components/schemas/AdminAuditLogEntry'

    AdminCreatorSummary:
      type: object
      properties:
        creatorId:
          type: string
        displayName:
          type: string
        walletAddress:
          type: string
        status:
          $ref: '#/components/schemas/AdminUserStatus'
        reputationScore:
          type: integer
        reputationTier:
          $ref: '#/components/schemas/ReputationTier'
        totalCampaigns:
          type: integer
        totalEarnings:
          type: number
          format: double
        joinedAt:
          type: string
          format: date-time

    AdminCreatorDetail:
      type: object
      properties:
        summary:
          $ref: '#/components/schemas/AdminCreatorSummary'
        profile:
          $ref: '#/components/schemas/CreatorProfile'
        stats:
          type: object
          properties:
            campaignsCompleted:
              type: integer
            campaignsActive:
              type: integer
            avgCVPI:
              type: number
              format: double
            earningsTotal:
              type: number
              format: double
            earnings30d:
              type: number
              format: double
        recentActivity:
          type: array
          items:
            $ref: '#/components/schemas/AdminAuditLogEntry'

    AdminProjectSummary:
      type: object
      properties:
        projectId:
          type: string
        projectName:
          type: string
        walletAddress:
          type: string
        status:
          $ref: '#/components/schemas/AdminUserStatus'
        reputationScore:
          type: integer
        reputationTier:
          $ref: '#/components/schemas/ReputationTier'
        totalCampaigns:
          type: integer
        totalBudgetSpent:
          type: number
          format: double
        avgCampaignCVPI:
          type: number
          format: double
        joinedAt:
          type: string
          format: date-time

    AdminProjectDetail:
      type: object
      properties:
        summary:
          $ref: '#/components/schemas/AdminProjectSummary'
        profile:
          type: object
          properties:
            bio:
              type: string
            website:
              type: string
            socialLinks:
              type: array
              items:
                $ref: '#/components/schemas/SocialLink'
            verificationStatus:
              type: string
        stats:
          type: object
          properties:
            campaignsCreated:
              type: integer
            budgetSpent:
              type: number
              format: double
            successRate:
              type: number
              format: double
            creatorsEngaged:
              type: integer
        recentActivity:
          type: array
          items:
            $ref: '#/components/schemas/AdminAuditLogEntry'

    AdminCampaignSummary:
      type: object
      properties:
        campaignId:
          type: string
        name:
          type: string
        projectName:
          type: string
        status:
          $ref: '#/components/schemas/CampaignStatus'
        budget:
          type: number
          format: double
        applications:
          type: integer
        approvedApplications:
          type: integer
        cvpi:
          type: number
          format: double
        createdAt:
          type: string
          format: date-time
        flagged:
          type: boolean

    AdminDisputeSummary:
      type: object
      properties:
        disputeId:
          type: string
        status:
          $ref: '#/components/schemas/DisputeStatus'
        issueType:
          $ref: '#/components/schemas/DisputeIssueType'
        campaignId:
          type: string
        campaignName:
          type: string
        initiator:
          type: string
        respondent:
          type: string
        amount:
          type: number
          format: double
        filedAt:
          type: string
          format: date-time
        escalated:
          type: boolean

    AdminDisputeDetail:
      allOf:
        - $ref: '#/components/schemas/AdminDisputeSummary'
        - type: object
          properties:
            validatorsAssigned:
              type: integer
            votes:
              type: array
              items:
                type: object
                properties:
                  validator:
                    type: string
                  stakingTier:
                    type: string
                  vote:
                    type: string
                  reason:
                    type: string
                  timestamp:
                    type: string
                    format: date-time
            evidence:
              type: object
              properties:
                project:
                  type: array
                  items:
                    type: string
                creator:
                  type: array
                  items:
                    type: string
            activity:
              type: array
              items:
                $ref: '#/components/schemas/AdminAuditLogEntry'

    AdminSystemHealth:
      type: object
      properties:
        overallStatus:
          type: string
        uptime30d:
          type: number
          format: double
        services:
          type: array
          items:
            $ref: '#/components/schemas/AdminServiceHealth'

    AdminServiceHealth:
      type: object
      properties:
        name:
          type: string
        status:
          type: string
          enum: [healthy, degraded, down]
        latencyMs:
          type: number
          format: double
        lastCheck:
          type: string
          format: date-time

    AdminAuditLogEntry:
      type: object
      properties:
        timestamp:
          type: string
          format: date-time
        adminUser:
          type: string
        actionType:
          type: string
        entityType:
          type: string
        entityId:
          type: string
        details:
          type: string
        ipAddress:
          type: string

    AdminFeeTier:
      type: object
      properties:
        range:
          type: string
        baseRate:
          type: number
          format: double
        lastUpdated:
          type: string
          format: date

    AdminFeeMultipliers:
      type: object
      properties:
        complexity:
          type: object
          properties:
            low:
              type: number
              format: double
            medium:
              type: number
              format: double
            high:
              type: number
              format: double
        reputationDiscounts:
          type: object
          properties:
            sTier:
              type: number
              format: double
            aTier:
              type: number
              format: double
            bTier:
              type: number
              format: double
            cTier:
              type: number
              format: double
        tokenDiscount:
          type: number
          format: double
        oracleFee:
          type: object
          properties:
            base:
              type: number
              format: double
            perAdditionalKPI:
              type: number
              format: double

    AdminDistributionRule:
      type: object
      properties:
        category:
          type: string
        percentage:
          type: number
          format: double
        amount30d:
          type: number
          format: double
        status:
          type: string
        lastChanged:
          type: string
          format: date

    AdminRevenueSummary:
      type: object
      properties:
        totalRevenue30d:
          type: number
          format: double
        totalCampaigns:
          type: integer
        avgRevenuePerCampaign:
          type: number
          format: double
        treasuryBalance:
          type: number
          format: double
        distribution:
          type: array
          items:
            $ref: '#/components/schemas/AdminDistributionRule'

    AdminCVPIAlgorithm:
      type: object
      properties:
        version:
          type: string
        effectiveDate:
          type: string
          format: date
        formula:
          type: string
        components:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              weight:
                type: number
                format: double
              metrics:
                type: array
                items:
                  type: string
              description:
                type: string

    AdminCVPIBenchmark:
      type: object
      properties:
        vertical:
          type: string
        medianCVPI:
          type: number
          format: double
        averageCVPI:
          type: number
          format: double
        campaigns:
          type: integer
        trend:
          type: number
          format: double

    AdminCVPIAnalytics:
      type: object
      properties:
        totalCampaigns:
          type: integer
        completed:
          type: integer
        cvpiCalculated:
          type: integer
        mean:
          type: number
          format: double
        median:
          type: number
          format: double
        stdDev:
          type: number
          format: double
        p10:
          type: number
          format: double
        p25:
          type: number
          format: double
        p75:
          type: number
          format: double
        p90:
          type: number
          format: double
        distribution:
          type: array
          items:
            type: object
            properties:
              range:
                type: string
              count:
                type: integer
              percentage:
                type: number
                format: double

    AdminSearchResult:
      type: object
      properties:
        type:
          type: string
          enum: [creator, project, campaign, dispute]
        id:
          type: string
        name:
          type: string
        status:
          type: string
        relevance:
          type: number
          format: double

    AdminStatusUpdateRequest:
      type: object
      required: [status, reason]
      properties:
        status:
          $ref: '#/components/schemas/AdminUserStatus'
        reason:
          type: string

    AdminReputationAdjustRequest:
      type: object
      required: [delta, reason]
      properties:
        delta:
          type: integer
        reason:
          type: string
        effectiveAt:
          type: string
          format: date-time

    AdminForceActionRequest:
      type: object
      required: [action, reason]
      properties:
        action:
          $ref: '#/components/schemas/AdminForceAction'
        reason:
          type: string

    AdminDisputeResolutionRequest:
      type: object
      required: [outcome, reason]
      properties:
        outcome:
          $ref: '#/components/schemas/DisputeResolutionOutcome'
        allocation:
          type: object
          properties:
            favorCreatorAmount:
              type: number
              format: double
            favorProjectAmount:
              type: number
              format: double
        reason:
          type: string
        notifyParties:
          type: boolean

    AdminPauseRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
        require2FA:
          type: boolean
          default: true

    AdminResumeRequest:
      type: object
      required: [reason]
      properties:
        reason:
          type: string
        require2FA:
          type: boolean
          default: true

paths:
  # ============ AUTH ENDPOINTS ============
  /auth/wallet-connect:
    post:
      tags: [Auth]
      summary: Initiate AW3 platform login via Privy (wallet authentication)
      description: |
        **Purpose:** Authenticate user to AW3 platform using wallet signature through Privy.
        
        **Authentication Flow:**
        1. User connects wallet (MetaMask, WalletConnect, etc.)
        2. System generates a nonce for signature
        3. User signs nonce with their wallet
        4. Backend verifies signature via `/auth/verify-signature`
        5. JWT tokens issued for AW3 platform access
        
        **This is NOT for social verification:**
        - This endpoint only handles AW3 platform authentication
        - Social account verification (Twitter, Discord, Telegram) is handled by a completely separate service
        - See `/creator/profile/social-verification` and `/social/*` endpoints for social verification
      operationId: walletConnect
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WalletConnectRequest'
      responses:
        '200':
          description: Nonce generated successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/WalletConnectResponse'

  /auth/verify-signature:
    post:
      tags: [Auth]
      summary: Verify wallet signature and issue AW3 platform access tokens
      description: |
        **Purpose:** Complete wallet-based authentication to AW3 platform via Privy.
        
        **What this does:**
        - Verifies the wallet signature against the nonce from `/auth/wallet-connect`
        - Issues JWT access token and refresh token for AW3 platform
        - Creates or updates user session
        
        **What this does NOT do:**
        - Does NOT verify social media accounts (Twitter, Discord, Telegram)
        - Does NOT fetch social metrics (followers, engagement)
        - Does NOT perform OAuth to social platforms
        
        **For social verification:**
        Social account verification is handled by a completely independent backend service with its own OAuth flows. After logging into AW3 via this endpoint, users can separately verify their social accounts through `/creator/profile/social-verification` or `/social/*` endpoints.
      operationId: verifySignature
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifySignatureRequest'
      responses:
        '200':
          description: Authentication successful
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/AuthResponse'

  /auth/register:
    post:
      tags: [Auth]
      summary: Register new user on AW3 platform
      description: |
        **Purpose:** Create a new user account on the AW3 platform after Privy authentication.
        
        **Prerequisites:**
        User must be authenticated via Privy using one of:
        - Gmail (social login via Privy)
        - Twitter (social login via Privy - NOTE: This is for AW3 platform login, NOT social verification)
        - Wallet (MetaMask, WalletConnect, etc.)
        
        **Registration Flow:**
        1. User authenticates to AW3 via Privy (email/social/wallet)
        2. This endpoint registers user with essential fields (role, wallet address, terms acceptance)
        3. Optional profile fields completed later via `/creator/profile/me` PUT endpoint
        
        **Important Distinction:**
        - Privy Twitter login = User logs into AW3 platform using their Twitter account (via Privy OAuth)
        - Social Verification = User proves ownership of Twitter/Discord/Telegram for campaign metrics
        
        These are TWO SEPARATE processes:
        - A user can log into AW3 with Gmail (via Privy) but verify their Twitter account for social metrics
        - A user can log into AW3 with Wallet but verify multiple social platforms (Twitter, Discord, Telegram)
        - Social verification is handled by an independent service at `/creator/profile/social-verification`
      operationId: register
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegistrationRequest'
      responses:
        '200':
          description: Registration successful
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RegistrationResponse'

  /auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh AW3 platform access token
      description: |
        Refresh the JWT access token for continued access to AW3 platform APIs.
        
        **Note:** This only refreshes AW3 platform authentication tokens.
        Social verification tokens (if any) are managed separately by the social verification service.
      operationId: refreshToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - refreshToken
              properties:
                refreshToken:
                  type: string
      responses:
        '200':
          description: Token refreshed
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  /auth/logout:
    post:
      tags: [Auth]
      summary: Logout from AW3 platform
      description: |
        Logout user from AW3 platform and invalidate access/refresh tokens.
        
        **Note:** This only logs out from AW3 platform. 
        Social account verifications remain valid and do not need to be re-verified after logout/login.
      operationId: logout
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Logout successful

  /auth/nonce/{walletAddress}:
    get:
      tags: [Auth]
      summary: Get nonce for wallet authentication to AW3 platform
      description: |
        Retrieve or generate a nonce for wallet signature authentication to AW3 platform.
        
        **Purpose:** Platform authentication only, not for social verification.
      operationId: getNonce
      parameters:
        - name: walletAddress
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Nonce retrieved

  # ============ CREATOR PROFILE ENDPOINTS ============
  /creator/profile/me:
    get:
      tags: [Creator Profile]
      summary: Get creator profile
      description: Retrieve authenticated creator's profile with focusArea instead of category
      operationId: getCreatorProfile
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Profile retrieved
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CreatorProfile'
    put:
      tags: [Creator Profile]
      summary: Update creator profile
      description: Update profile information (no socialLinks field)
      operationId: updateCreatorProfile
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatorProfileUpdateRequest'
      responses:
        '200':
          description: Profile updated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CreatorProfile'

  /creator/profile/social-verification:
    post:
      tags: [Creator Profile]
      summary: Initiate social account verification (independent service)
      description: |
        **CRITICAL ARCHITECTURE NOTE: This is a COMPLETELY SEPARATE service from Privy authentication**
        
        **Purpose:**
        Verify ownership of social media accounts (Twitter, Discord, Telegram) and fetch metrics (followers, engagement rates) for campaign matching and CVPI calculation.
        
        **Architecture:**
        1. **Independent Backend Service**: Social verification is handled by a dedicated microservice, NOT Privy
        2. **Own OAuth Implementation**: This service implements its own OAuth 2.0 flows to each platform
        3. **Direct Platform API Calls**: Fetches metrics directly from Twitter API, Discord API, Telegram API
        4. **No Privy Dependency**: Does NOT use Privy for social OAuth or token management
        
        **Verification Flow:**
        1. User (already logged into AW3 via Privy) requests social verification
        2. Social verification service generates platform-specific OAuth URL
        3. User redirects to Twitter/Discord/Telegram for authorization
        4. Platform redirects back to social verification service callback
        5. Service exchanges authorization code for access token (own OAuth implementation)
        6. Service fetches social metrics (followers, engagement) from platform API
        7. Service stores verification data (hashed user ID, public metrics only)
        8. Tokens discarded after initial fetch (MVP: one-time verification, no continuous monitoring)
        
        **What Gets Stored:**
        - Platform (Twitter/Discord/Telegram)
        - Handle/username
        - Follower count (at verification time)
        - Engagement rate (calculated from public posts)
        - Verification timestamp
        
        **What Does NOT Get Stored:**
        - OAuth access/refresh tokens (discarded after fetch in MVP)
        - Private messages or DMs
        - Email, phone, or personal information
        
        **Example Scenario:**
        - User logs into AW3 with Gmail (via Privy) ← Platform authentication
        - User then verifies Twitter account (via independent social verification service) ← Social metrics
        - These are TWO COMPLETELY SEPARATE authentication flows
        
        **Related Endpoints:**
        For the full OAuth flow, see:
        - `GET /social/{platform}/authorize-url` - Get OAuth URL for platform
        - `GET /social/callback` - Handle OAuth callback from platform
      operationId: verifySocialAccount
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - platform
                - handle
              properties:
                platform:
                  $ref: '#/components/schemas/SocialPlatform'
                handle:
                  type: string
                  example: "@cryptoinfluencer"
                  description: Social media handle to verify
                verificationCode:
                  type: string
                  description: Optional verification code (if using code-based verification instead of OAuth)
      responses:
        '200':
          description: |
            Social verification initiated successfully via the independent social verification backend service.
            
            The service will:
            1. Perform OAuth with Twitter/Discord/Telegram (own implementation, NOT Privy)
            2. Fetch metrics (followers, engagement) directly from platform APIs
            3. Store verification data (no long-term token storage in MVP)
            
            This flow is completely decoupled from Privy authentication.

  # ============ SOCIAL VERIFICATION SERVICE ENDPOINTS ============
  # These endpoints are part of the INDEPENDENT social verification service
  # NOT part of Privy authentication - completely separate OAuth implementation
  
  /social/{platform}/authorize-url:
    get:
      tags: [Creator Profile]
      summary: Get OAuth authorization URL for social platform
      description: |
        **Independent Social Verification Service**
        
        Generate platform-specific OAuth authorization URL for social account verification.
        This is handled by the independent social verification service, NOT Privy.
        
        **Architecture:**
        - Independent backend service generates OAuth URL with PKCE
        - Stores state parameter and code verifier in session/Redis
        - Returns URL for frontend to redirect user to platform
        
        **Flow:**
        1. Frontend calls this endpoint
        2. Backend generates OAuth URL with proper scopes (read profile, followers)
        3. Frontend redirects user to returned URL
        4. User authorizes on Twitter/Discord/Telegram
        5. Platform redirects to `/social/callback`
        
        **Supported Platforms:**
        - twitter: Twitter/X OAuth 2.0
        - discord: Discord OAuth 2.0
        - telegram: Telegram Bot OAuth
        
        **Important:**
        This OAuth flow is completely separate from Privy. If a user logged into AW3 using Twitter via Privy, they still need to go through this separate OAuth flow to verify their Twitter account for social metrics.
      operationId: getSocialAuthUrl
      security:
        - bearerAuth: []
      parameters:
        - name: platform
          in: path
          required: true
          schema:
            type: string
            enum: [twitter, discord, telegram, instagram, youtube, tiktok]
          example: twitter
      responses:
        '200':
          description: OAuth authorization URL generated successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          authUrl:
                            type: string
                            example: "https://twitter.com/i/oauth2/authorize?response_type=code&client_id=xxx&redirect_uri=https://api.aw3.io/social/callback&scope=tweet.read%20users.read%20offline.access&state=xxx&code_challenge=xxx&code_challenge_method=S256"
                            description: OAuth authorization URL - frontend should redirect user to this URL
                          state:
                            type: string
                            description: CSRF protection state parameter (stored server-side)
                          platform:
                            type: string
                            example: twitter
        '401':
          description: User not authenticated to AW3 platform
        '400':
          description: Invalid or unsupported platform

  /social/callback:
    get:
      tags: [Creator Profile]
      summary: Handle OAuth callback from social platform
      description: |
        **Independent Social Verification Service - OAuth Callback Handler**
        
        This endpoint receives the OAuth callback from Twitter/Discord/Telegram after user authorization.
        Handled entirely by the independent social verification service, NOT Privy.
        
        **Backend Processing:**
        1. Validates state parameter (CSRF protection)
        2. Exchanges authorization code for access token (own OAuth implementation)
        3. Fetches user profile and metrics from platform API
        4. Stores verification data (hashed user ID, public metrics)
        5. Discards tokens (MVP: one-time verification only)
        6. Redirects user back to AW3 frontend with success/error
        
        **Twitter API Calls (Example):**
        - POST https://api.twitter.com/2/oauth2/token (exchange code for token)
        - GET https://api.twitter.com/2/users/me?user.fields=public_metrics (fetch profile and followers)
        
        **Data Fetched:**
        - Twitter: followers_count, following_count, tweet_count, engagement rate
        - Discord: username, discriminator, guilds owned, total members
        - Telegram: username, channel subscribers
        
        **Data Storage:**
        - Platform and handle (for display)
        - Follower count (at verification time)
        - Verification timestamp
        - Hashed platform user ID (privacy protection)
        - NO OAuth tokens stored in MVP (one-time verification)
        
        **Security:**
        - State parameter validation prevents CSRF attacks
        - PKCE code verifier prevents authorization code interception
        - All token exchange happens server-side only
        - Tokens never exposed to frontend
      operationId: handleSocialCallback
      parameters:
        - name: code
          in: query
          required: true
          schema:
            type: string
          description: Authorization code from OAuth provider
        - name: state
          in: query
          required: true
          schema:
            type: string
          description: State parameter for CSRF protection
        - name: error
          in: query
          schema:
            type: string
          description: Error code if user denied authorization
        - name: error_description
          in: query
          schema:
            type: string
          description: Human-readable error description
      responses:
        '302':
          description: |
            Redirect to AW3 frontend with verification result
            
            Success: /creator/profile?social_verification=success&platform=twitter
            Error: /creator/profile?social_verification=error&platform=twitter&error=access_denied
        '400':
          description: Invalid state parameter or authorization code
        '500':
          description: Failed to fetch metrics from platform API

  /social/status:
    get:
      tags: [Creator Profile]
      summary: Get social verification status for all platforms
      description: |
        Retrieve verification status and metrics for all social platforms linked to the authenticated creator.
        
        **Returns:**
        - List of verified social accounts
        - Current follower counts (as of last verification)
        - Verification timestamps
        - Data freshness indicators
        
        **Note:** In MVP, data is from one-time verification. Shows "X days old" indicator.
      operationId: getSocialStatus
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Social verification status retrieved
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          socialAccounts:
                            type: array
                            items:
                              type: object
                              properties:
                                platform:
                                  type: string
                                  enum: [twitter, discord, telegram, instagram, youtube]
                                handle:
                                  type: string
                                  example: "@cryptoinfluencer"
                                verified:
                                  type: boolean
                                  example: true
                                followersCount:
                                  type: integer
                                  example: 125000
                                engagementRate:
                                  type: number
                                  format: float
                                  example: 8.5
                                  description: Percentage engagement rate
                                verifiedAt:
                                  type: string
                                  format: date-time
                                  example: "2025-12-15T08:30:00Z"
                                daysOld:
                                  type: integer
                                  example: 5
                                  description: Days since verification (data freshness indicator)
                                requiresReverification:
                                  type: boolean
                                  example: false
                                  description: True if data is older than 90 days

  /social/{platform}/disconnect:
    delete:
      tags: [Creator Profile]
      summary: Disconnect and remove social account verification
      description: |
        Remove social account verification data for the specified platform.
        
        **What gets deleted:**
        - Verification record
        - Stored metrics (followers, engagement)
        - Any associated data
        
        **GDPR/Privacy Compliance:**
        This endpoint supports user's right to deletion.
      operationId: disconnectSocialAccount
      security:
        - bearerAuth: []
      parameters:
        - name: platform
          in: path
          required: true
          schema:
            type: string
            enum: [twitter, discord, telegram, instagram, youtube, tiktok]
      responses:
        '200':
          description: Social account disconnected successfully
        '404':
          description: No verification found for this platform

  # ============ CREATOR CAMPAIGNS ENDPOINTS ============
  /creator/campaigns:
    get:
      tags: [Creator Campaigns]
      summary: Browse active campaigns with UI-aligned filtering
      description: |
        Returns campaigns with only essential overview fields. Detailed data available in campaign detail endpoint.
        
        **Important Note:**
        This endpoint does NOT include matchRate in the response. The server cannot calculate match rate because it requires comparison between the user's profile and each campaign's criteria. Match rate must be calculated on the frontend based on:
        - User's profile data (reputation, follower count, verified accounts, etc.)
        - Campaign criteria (requirements, target audience, qualifications)
        
        **UI Alignment:**
        All filter parameters support multi-select to match UI behavior. Frontend should send multiple values as comma-separated or repeated query parameters.
        
        **Example Query:**
        ```
        GET /creator/campaigns?keyword=NFT&vertical=2&vertical=3&deliverables=1&deliverables=2&duration=1&duration=2&stage=2&page=0&size=20
        ```
        
        **Parameter Mappings:**
        - vertical: 1=DeFi, 2=NFT, 3=Gaming, 4=Infrastructure, 5=L2, 6=DAO, 7=Metaverse, 8=Trading, 9=Other
        - deliverables: 1=Twitter Posts, 2=Videos, 3=Articles, 4=AMAs, 5=Discord Management, 6=Community Giveaways, 7=Instagram Post, 8=TikTok
        - duration: 1=Less Than 1 Week, 2=1-2 Weeks, 3=2-4 Weeks, 4=1-3 Months, 5=3+ Months
        - stage: 1=Ideation, 2=Live Configuration, 3=Notification Preferences, 4=Privacy Controls, 5=Wallet & Payout, 6=Security
      operationId: browseCreatorCampaigns
      security:
        - bearerAuth: []
      parameters:
        - name: keyword
          in: query
          description: |
            Keyword search against campaign title and description.
            UI: Search input field at top of campaigns list.
          required: false
          schema:
            type: string
            example: "NFT marketing"
        - name: vertical
          in: query
          description: |
            Filter by vertical/focus area (multi-select).
            UI: "Vertical" filter section with checkboxes.
            Values: 1=DeFi, 2=NFT, 3=Gaming, 4=Infrastructure, 5=L2, 6=DAO, 7=Metaverse, 8=Trading, 9=Other
          required: false
          schema:
            type: array
            items:
              $ref: '#/components/schemas/FocusArea'
          style: form
          explode: true
          example: [2, 3]
        - name: deliverables
          in: query
          description: |
            Filter by deliverable types (multi-select).
            UI: "Deliverables" filter section with checkboxes.
            Values: 1=Twitter Posts, 2=Videos, 3=Articles, 4=AMAs, 5=Discord Management, 6=Community Giveaways, 7=Instagram Post, 8=TikTok
          required: false
          schema:
            type: array
            items:
              $ref: '#/components/schemas/DeliverableType'
          style: form
          explode: true
          example: [1, 2]
        - name: duration
          in: query
          description: |
            Filter by campaign duration (multi-select).
            UI: "Duration" filter section with checkboxes.
            Values: 1=Less Than 1 Week, 2=1-2 Weeks, 3=2-4 Weeks, 4=1-3 Months, 5=3+ Months
          required: false
          schema:
            type: array
            items:
              $ref: '#/components/schemas/CampaignDuration'
          style: form
          explode: true
          example: [1, 2]
        - name: stage
          in: query
          description: |
            Filter by campaign stage (multi-select).
            UI: "Stage" filter section with checkboxes.
            Values: 1=Ideation, 2=Live Configuration, 3=Notification Preferences, 4=Privacy Controls, 5=Wallet & Payout, 6=Security
          required: false
          schema:
            type: array
            items:
              $ref: '#/components/schemas/CampaignStage'
          style: form
          explode: true
          example: [2, 3]
        - name: page
          in: query
          description: Page number for pagination (zero-indexed)
          required: false
          schema:
            type: integer
            default: 0
            minimum: 0
          example: 0
        - name: size
          in: query
          description: Number of items per page
          required: false
          schema:
            type: integer
            default: 20
            minimum: 1
            maximum: 100
          example: 20
      responses:
        '200':
          description: Campaigns list with simplified fields
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          campaigns:
                            type: array
                            items:
                              $ref: '#/components/schemas/CampaignListItem'
                          pagination:
                            $ref: '#/components/schemas/Pagination'

  /creator/campaigns/{id}:
    get:
      tags: [Creator Campaigns]
      summary: Get campaign details with full information
      description: Returns complete campaign information including project info, objective, and payment terms
      operationId: getCreatorCampaignDetails
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Campaign details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CampaignDetail'

  # ============ CREATOR APPLICATIONS ENDPOINTS ============
  /creator/applications:
    get:
      tags: [Creator Applications]
      summary: List own applications
      operationId: listCreatorApplications
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
        - name: size
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: Applications list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: array
                        items:
                          $ref: '#/components/schemas/Application'
    post:
      tags: [Creator Applications]
      summary: Submit application
      operationId: submitApplication
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApplicationRequest'
      responses:
        '200':
          description: Application submitted
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Application'

  /creator/applications/{id}:
    get:
      tags: [Creator Applications]
      summary: Get application details
      operationId: getCreatorApplication
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Application details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/Application'

  # ============ CREATOR DELIVERABLES ENDPOINTS ============
  /creator/deliverables:
    get:
      tags: [Creator Deliverables]
      summary: List deliverables
      operationId: listCreatorDeliverables
      security:
        - bearerAuth: []
      parameters:
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/DeliverableStatus'
      responses:
        '200':
          description: Deliverables list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  # ============ CREATOR EARNINGS ENDPOINTS ============
  /creator/earnings:
    get:
      tags: [Creator Earnings]
      summary: Get earnings summary with ROI and growth rate
      description: Returns comprehensive earnings data including averageROI and growthRate
      operationId: getCreatorEarnings
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Earnings summary
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/EarningsSummary'

  /creator/earnings/history:
    get:
      tags: [Creator Earnings]
      summary: Get earnings history chart data
      description: Returns time-series data for chart visualization over last 30 days
      operationId: getCreatorEarningsHistory
      security:
        - bearerAuth: []
      parameters:
        - name: range
          in: query
          required: true
          description: Time range for chart data
          schema:
            $ref: '#/components/schemas/EarningsRange'
      responses:
        '200':
          description: Earnings history chart data
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/EarningsHistoryChart'

  # ============ CREATOR CVPI ENDPOINTS ============
  /creator/cvpi/score:
    get:
      tags: [Creator CVPI]
      summary: Get CVPI score with integrated reputation data
      description: Returns CVPI score including reputation metrics (score, tier, reviews, rating)
      operationId: getCreatorCVPI
      security:
        - bearerAuth: []
      responses:
        '200':
          description: CVPI score with reputation
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CVPIScore'

  /creator/cvpi/history:
    get:
      tags: [Creator CVPI]
      summary: Get CVPI history with reputation records
      description: Returns historical CVPI and reputation data
      operationId: getCreatorCVPIHistory
      security:
        - bearerAuth: []
      parameters:
        - name: period
          in: query
          required: true
          description: Time period for history
          schema:
            $ref: '#/components/schemas/TimePeriod'
        - name: limit
          in: query
          required: true
          description: Maximum number of records to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            example: 30
      responses:
        '200':
          description: CVPI history
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CVPIHistory'

  # ============ CREATOR CERTIFICATES ENDPOINTS ============
  /creator/certificates:
    get:
      tags: [Creator Certificates]
      summary: Get creator certificates and achievements
      description: Returns all certificates earned by the creator
      operationId: getCreatorCertificates
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Certificates list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          certificates:
                            type: array
                            items:
                              $ref: '#/components/schemas/Certificate'

  # ============ CREATOR SETTINGS ENDPOINTS ============
  /creator/settings/language:
    get:
      tags: [Creator Settings]
      summary: Get language settings
      operationId: getLanguageSettings
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Language settings
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/LanguageSettings'
    post:
      tags: [Creator Settings]
      summary: Update language settings
      operationId: updateLanguageSettings
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LanguageSettings'
      responses:
        '200':
          description: Settings updated

  /creator/system/localization:
    get:
      tags: [Creator System]
      summary: Get localization literals (languages and timezones)
      operationId: getCreatorLocalizationLiterals
      responses:
        '200':
          description: Localization literals for language and timezone selection
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          languages:
                            type: array
                            items:
                              type: object
                              required: [code, label]
                              properties:
                                code:
                                  type: string
                                label:
                                  type: string
                            example:
                              - code: en
                                label: English
                              - code: en-US
                                label: English (United States)
                              - code: zh-CN
                                label: Chinese (Simplified, China)
                              - code: zh-TW
                                label: Chinese (Traditional, Taiwan)
                              - code: ja-JP
                                label: Japanese (Japan)
                              - code: ko-KR
                                label: Korean (South Korea)
                          timezones:
                            type: array
                            items:
                              type: string
                            example:
                              - America/New_York
                              - Europe/London
                              - Asia/Shanghai
                              - Asia/Tokyo
                              - Asia/Seoul

  /creator/settings/rate:
    get:
      tags: [Creator Settings]
      summary: Get rate configuration
      operationId: getRateConfiguration
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Rate configuration
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/RateConfiguration'
    post:
      tags: [Creator Settings]
      summary: Update rate configuration
      operationId: updateRateConfiguration
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RateConfiguration'
      responses:
        '200':
          description: Rate configuration updated

  /creator/settings/notification:
    get:
      tags: [Creator Settings]
      summary: Get notification preferences
      description: |
        Get notification preferences for email and push notifications.
        
        **Email Notifications:**
        - New campaign matches
        - Application status updates
        - Payment received
        - Messages from clients
        - Community interactions
        - Platform announcements
        
        **Push Notifications:**
        - Real-time messages
        - Campaign deadlines
        - Payment confirmations
        - Milestone reminders
        
        **Notification Frequency:**
        - instant: Receive notifications immediately
        - daily_digest: Once per day summary
        - weekly_summary: Once per week summary
      operationId: getNotificationSettings
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Notification settings retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/NotificationSettings'
              example:
                success: true
                data:
                  email:
                    newCampaignMatches: true
                    applicationStatusUpdates: false
                    paymentReceived: true
                    messagesFromClients: true
                    communityInteractions: false
                    platformAnnouncements: false
                  push:
                    realTimeMessages: true
                    campaignDeadlines: true
                    paymentConfirmations: false
                    milestoneReminders: false
                  frequency: instant
    post:
      tags: [Creator Settings]
      summary: Update notification preferences
      description: |
        Update notification preferences for email and push notifications.
        
        All fields are required in the request body. Set boolean values to true/false to enable/disable specific notification types.
      operationId: updateNotificationSettings
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NotificationSettings'
            example:
              email:
                newCampaignMatches: true
                applicationStatusUpdates: false
                paymentReceived: true
                messagesFromClients: true
                communityInteractions: false
                platformAnnouncements: false
              push:
                realTimeMessages: true
                campaignDeadlines: true
                paymentConfirmations: false
                milestoneReminders: false
              frequency: instant
      responses:
        '200':
          description: Notification settings updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              example:
                success: true
                message: Notification settings updated successfully

  /creator/settings/privacy:
    get:
      tags: [Creator Settings]
      summary: Get privacy settings
      description: |
        Get privacy settings for profile visibility and information sharing.
        
        **Profile Visibility Options:**
        - PUBLIC: Profile visible to everyone
        - PRIVATE: Profile only visible to you
        - CONNECTIONS_ONLY: Profile visible to connected users only
        
        **Information Sharing:**
        - showEarnings: Display earnings on public profile
        - showCompletedCampaigns: Display completed campaign count
        - showSocialAccounts: Display verified social accounts
      operationId: getPrivacySettings
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Privacy settings retrieved successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/PrivacySettings'
              example:
                success: true
                data:
                  profileVisibility: PUBLIC
                  showEarnings: false
                  showCompletedCampaigns: true
                  showSocialAccounts: true
    post:
      tags: [Creator Settings]
      summary: Update privacy settings
      description: |
        Update privacy settings for profile visibility and information sharing.
        
        profileVisibility is required. Other fields are optional and default to false if not provided.
      operationId: updatePrivacySettings
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PrivacySettings'
            example:
              profileVisibility: PUBLIC
              showEarnings: false
              showCompletedCampaigns: true
              showSocialAccounts: true
      responses:
        '200':
          description: Privacy settings updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
              example:
                success: true
                message: Privacy settings updated successfully

  /creator/settings/security:
    get:
      tags: [Creator Settings]
      summary: Get security settings (Privy-managed)
      description: |
        **IMPORTANT: Security and Wallet settings are managed by Privy.**
        
        This endpoint is deprecated. Security features including:
        - Two-factor authentication (2FA)
        - Login alerts
        - Trusted devices
        - Wallet management
        
        Are all handled by Privy's authentication system. Please use Privy's dashboard and APIs for security management.
        
        For security settings, direct users to:
        - Privy Dashboard: https://dashboard.privy.io
        - Privy Security Docs: https://docs.privy.io/security/overview
      deprecated: true
      operationId: getSecuritySettings
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Security settings (managed by Privy)
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          message:
                            type: string
                            example: Security settings are managed by Privy. Please use Privy dashboard for security configuration.
                          privyDashboard:
                            type: string
                            example: https://dashboard.privy.io
    post:
      tags: [Creator Settings]
      summary: Update security settings (Privy-managed)
      description: |
        **IMPORTANT: Security settings are managed by Privy.**
        
        This endpoint is deprecated. Please use Privy's authentication system to manage security settings.
      deprecated: true
      operationId: updateSecuritySettings
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SecuritySettings'
      responses:
        '200':
          description: Settings updated

  # ============ DASHBOARD ENDPOINTS ============
  /dashboard/trending:
    get:
      tags: [Dashboard]
      summary: Get trending campaigns
      description: Returns popular or trending campaigns
      operationId: getDashboardTrending
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Trending campaigns
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/DashboardTrending'

  /dashboard/live:
    get:
      tags: [Dashboard]
      summary: Get live campaigns
      description: Returns active ongoing campaigns
      operationId: getDashboardLive
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Live campaigns
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/DashboardLive'

  /dashboard/action-items:
    get:
      tags: [Dashboard]
      summary: Get action items
      description: Returns tasks requiring creator attention
      operationId: getDashboardActionItems
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Action items
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/DashboardActionItems'

  /dashboard/analytics:
    get:
      tags: [Dashboard]
      summary: Get footer analytics data
      description: Returns 4 key analytics metrics for footer display
      operationId: getDashboardAnalytics
      responses:
        '200':
          description: Footer analytics
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/FooterAnalytics'

  # ============ FILTER ENDPOINTS ============
  /filters/campaign-options:
    get:
      tags: [Filters]
      summary: Get all filter options for campaign search
      description: Returns all available filter options with counts
      operationId: getCampaignFilterOptions
      responses:
        '200':
          description: Filter options
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/FilterOptions'

  # ============ PUBLIC MARKETPLACE ENDPOINTS ============
  /public/marketplace/campaigns:
    get:
      tags: [Public Marketplace]
      summary: Browse public campaigns
      operationId: getPublicCampaigns
      parameters:
        - name: focusArea
          in: query
          schema:
            $ref: '#/components/schemas/FocusArea'
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/CampaignStatus'
        - name: minBudget
          in: query
          schema:
            type: number
        - name: maxBudget
          in: query
          schema:
            type: number
        - name: page
          in: query
          schema:
            type: integer
        - name: size
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: Public campaigns
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  /public/marketplace/stats:
    get:
      tags: [Public Marketplace]
      summary: Get platform statistics
      operationId: getPlatformStats
      responses:
        '200':
          description: Platform stats
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  # ============ PROJECT DASHBOARD ENDPOINTS ============
  /project/dashboard/stats:
    get:
      tags: [Project Dashboard]
      summary: Get project dashboard statistics
      description: Returns key metrics for project dashboard including active campaigns, pending applications, budget available
      operationId: getProjectDashboardStats
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Dashboard statistics
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectDashboardStats'

  # ============ PROJECT CAMPAIGNS ENDPOINTS ============
  /project/campaigns:
    get:
      tags: [Project Campaigns]
      summary: List project campaigns
      description: Get all campaigns created by the project with filtering and sorting
      operationId: listProjectCampaigns
      security:
        - bearerAuth: []
      parameters:
        - name: status
          in: query
          description: Filter by campaign status
          schema:
            $ref: '#/components/schemas/CampaignStatus'
        - name: focusArea
          in: query
          description: Filter by vertical
          schema:
            $ref: '#/components/schemas/FocusArea'
        - name: minBudget
          in: query
          schema:
            type: number
        - name: maxBudget
          in: query
          schema:
            type: number
        - name: sort
          in: query
          description: Sort option
          schema:
            $ref: '#/components/schemas/ProjectCampaignSort'
        - name: page
          in: query
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Campaign list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          campaigns:
                            type: array
                            items:
                              $ref: '#/components/schemas/ProjectCampaignListItem'
                          pagination:
                            $ref: '#/components/schemas/Pagination'
    post:
      tags: [Project Campaigns]
      summary: Create new campaign
      description: Create a new campaign and lock funds in escrow via smart contract
      operationId: createProjectCampaign
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CampaignCreateRequest'
      responses:
        '200':
          description: Campaign creation transaction initiated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CampaignTransactionResponse'

  /project/campaigns/{id}:
    get:
      tags: [Project Campaigns]
      summary: Get campaign details
      operationId: getProjectCampaign
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Campaign details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CampaignDetail'
    put:
      tags: [Project Campaigns]
      summary: Update campaign (limited fields)
      description: Update editable campaign fields. Only certain fields can be edited after campaign is launched on-chain.
      operationId: updateProjectCampaign
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                description:
                  type: string
                contentGuidelines:
                  type: string
                coverImage:
                  type: string
                  format: uri
      responses:
        '200':
          description: Campaign updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'
    delete:
      tags: [Project Campaigns]
      summary: Delete draft campaign
      description: Delete campaign only if in draft status. Cannot delete campaigns on-chain.
      operationId: deleteProjectCampaign
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Campaign deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'

  /project/campaigns/{id}/pause:
    post:
      tags: [Project Campaigns]
      summary: Pause active campaign
      description: Temporarily pause campaign to stop accepting new applications
      operationId: pauseProjectCampaign
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Campaign paused
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'

  /project/campaigns/{id}/resume:
    post:
      tags: [Project Campaigns]
      summary: Resume paused campaign
      operationId: resumeProjectCampaign
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Campaign resumed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'

  /project/campaigns/{id}/extend:
    post:
      tags: [Project Campaigns]
      summary: Extend campaign duration
      description: Extend campaign end date
      operationId: extendProjectCampaign
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - newEndDate
              properties:
                newEndDate:
                  type: string
                  format: date
      responses:
        '200':
          description: Campaign extended
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'

  /project/campaigns/{id}/invite:
    post:
      tags: [Project Campaigns]
      summary: Invite specific creators to campaign
      operationId: inviteCreatorsToCampaign
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - creatorIds
              properties:
                creatorIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                message:
                  type: string
      responses:
        '200':
          description: Invitations sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'

  /project/campaigns/{id}/export:
    get:
      tags: [Project Campaigns]
      summary: Export campaign report
      description: Generate and download campaign report in PDF or Excel format
      operationId: exportCampaignReport
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: format
          in: query
          required: true
          schema:
            type: string
            enum: [PDF, EXCEL, CSV]
      responses:
        '200':
          description: Report file
          content:
            application/pdf:
              schema:
                type: string
                format: binary
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary

  /project/campaigns/{id}/metrics:
    get:
      tags: [Project Campaigns]
      summary: Get campaign key metrics
      description: Returns budget status, applications, progress, and CVPI score for campaign dashboard view
      operationId: getCampaignMetrics
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Campaign metrics
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CampaignMetrics'

  /project/campaigns/{id}/overview:
    get:
      tags: [Project Campaigns]
      summary: Get campaign overview tab data
      description: Returns campaign details, KPI targets, and approved creators for Overview tab
      operationId: getCampaignOverview
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Campaign overview
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  /project/campaigns/{id}/analytics:
    get:
      tags: [Project Analytics]
      summary: Get campaign analytics
      description: Returns comprehensive analytics including CVPI breakdown, KPI achievement, audience reach, and cost efficiency
      operationId: getCampaignAnalytics
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Campaign analytics
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CampaignAnalytics'

  /project/campaigns/{id}/financials:
    get:
      tags: [Project Finance]
      summary: Get campaign financial details
      description: Returns escrow overview, budget allocation, payment history, and fee breakdown
      operationId: getCampaignFinancials
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Campaign financials
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/CampaignFinancials'

  # ============ PROJECT APPLICATIONS ENDPOINTS ============
  /project/applications:
    get:
      tags: [Project Applications]
      summary: List applications for project campaigns
      description: Get all applications with filtering and sorting options
      operationId: listProjectApplications
      security:
        - bearerAuth: []
      parameters:
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          description: Filter by application status
          schema:
            $ref: '#/components/schemas/ApplicationStatus'
        - name: sort
          in: query
          description: Sort option
          schema:
            $ref: '#/components/schemas/ApplicationSort'
        - name: sortOrder
          in: query
          schema:
            type: string
            enum: [ASC, DESC]
            default: DESC
        - name: minReputation
          in: query
          schema:
            type: integer
        - name: page
          in: query
          schema:
            type: integer
            default: 0
        - name: size
          in: query
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Applications list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          applications:
                            type: array
                            items:
                              $ref: '#/components/schemas/ProjectApplication'
                          pagination:
                            $ref: '#/components/schemas/Pagination'

  /project/applications/{id}:
    get:
      tags: [Project Applications]
      summary: Get application details
      operationId: getProjectApplication
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Application details
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/ProjectApplication'

  /project/applications/approve:
    post:
      tags: [Project Applications]
      summary: Approve applications (batch)
      description: Approve one or multiple applications at once
      operationId: approveApplications
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BatchApprovalRequest'
      responses:
        '200':
          description: Approval results
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/BatchApprovalResponse'

  /project/applications/reject:
    post:
      tags: [Project Applications]
      summary: Reject applications (batch)
      description: Reject one or multiple applications at once
      operationId: rejectApplications
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              allOf:
                - $ref: '#/components/schemas/BatchApprovalRequest'
                - type: object
                  properties:
                    reason:
                      type: string
      responses:
        '200':
          description: Rejection results
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        $ref: '#/components/schemas/BatchApprovalResponse'

  /project/applications/{id}/deliverables:
    get:
      tags: [Project Deliverables]
      summary: Get deliverables for specific application
      operationId: getApplicationDeliverables
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deliverables list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  # ============ PROJECT DELIVERABLES ENDPOINTS ============
  /project/deliverables:
    get:
      tags: [Project Deliverables]
      summary: List deliverables for verification
      description: Get all submitted deliverables across campaigns
      operationId: listProjectDeliverables
      security:
        - bearerAuth: []
      parameters:
        - name: campaignId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/DeliverableStatus'
        - name: page
          in: query
          schema:
            type: integer
        - name: size
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: Deliverables list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  /project/deliverables/{id}:
    get:
      tags: [Project Deliverables]
      summary: Get deliverable details
      operationId: getProjectDeliverable
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Deliverable details with oracle verification results
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  /project/deliverables/{id}/verify:
    post:
      tags: [Project Deliverables]
      summary: Verify and release payment
      description: Approve deliverable and trigger payment release from escrow
      operationId: verifyDeliverable
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Payment release initiated
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  /project/deliverables/{id}/request-revision:
    post:
      tags: [Project Deliverables]
      summary: Request revision
      operationId: requestDeliverableRevision
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - feedback
              properties:
                feedback:
                  type: string
      responses:
        '200':
          description: Revision requested
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'

  /project/deliverables/{id}/reject:
    post:
      tags: [Project Deliverables]
      summary: Reject deliverable
      description: Reject deliverable with reason (may trigger dispute)
      operationId: rejectDeliverable
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Deliverable rejected
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiResponse'

  # ============ PROJECT CREATORS ENDPOINTS ============
  /project/creators/discover:
    get:
      tags: [Project Creators]
      summary: Discover creators
      description: Search and filter creators based on criteria with CVPI-focused results
      operationId: discoverCreators
      security:
        - bearerAuth: []
      parameters:
        - name: minReputation
          in: query
          schema:
            type: integer
        - name: maxReputation
          in: query
          schema:
            type: integer
        - name: minCVPI
          in: query
          schema:
            type: number
        - name: maxCVPI
          in: query
          schema:
            type: number
        - name: platform
          in: query
          schema:
            $ref: '#/components/schemas/SocialPlatform'
        - name: minFollowers
          in: query
          schema:
            type: integer
        - name: focusArea
          in: query
          schema:
            $ref: '#/components/schemas/FocusArea'
        - name: availableOnly
          in: query
          schema:
            type: boolean
            default: false
        - name: sort
          in: query
          schema:
            type: string
            enum: [BestCVPI, HighestReputation, MostFollowers, MostCampaigns]
            default: BestCVPI
        - name: page
          in: query
          schema:
            type: integer
        - name: size
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: Creators list
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'
                  - type: object
                    properties:
                      data:
                        type: object
                        properties:
                          creators:
                            type: array
                            items:
                              $ref: '#/components/schemas/CreatorDiscoveryResult'
                          pagination:
                            $ref: '#/components/schemas/Pagination'

  /project/creators/recommended:
    get:
      tags: [Project Creators]
      summary: Get recommended creators
      description: AI-recommended creators based on project campaign history and preferences
      operationId: getRecommendedCreators
      security:
        - bearerAuth: []
      parameters:
        - name: campaignId
          in: query
          description: Get recommendations for specific campaign
          schema:
            type: string
            format: uuid
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Recommended creators
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  /project/creators/{id}:
    get:
      tags: [Project Creators]
      summary: Get creator profile details
      operationId: getCreatorProfileForProject
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Creator profile
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

  # ============ PROJECT ANALYTICS ENDPOINTS ============
  /project/analytics/overview:
    get:
      tags: [Project Analytics]
      summary: Get analytics overview
      description: Returns comprehensive analytics across all campaigns
      operationId: getProjectAnalyticsOverview
      security:
        - bearerAuth: []
      parameters:
        - name: period
          in: query
          schema:
            $ref: '#/components/schemas/TimePeriod'
        - name: compareWith
          in: query
          schema:
            type: string
            enum: [PreviousPeriod, PlatformAverage, VerticalAverage]
      responses:
        '200':
          description: Analytics overview
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/ApiResponse'

# ============ ADMIN DASHBOARD ============
  /admin/dashboard/stats:
    get:
      tags: [Admin Dashboard]
      summary: Get admin dashboard statistics
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Dashboard statistics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDashboardStats'

# ============ ADMIN CREATORS ============
  /admin/creators:
    get:
      tags: [Admin Creators]
      summary: List creators with filters
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/AdminUserStatus'
        - in: query
          name: reputationMin
          schema:
            type: integer
        - in: query
          name: reputationMax
          schema:
            type: integer
        - in: query
          name: joinDateFrom
          schema:
            type: string
            format: date
        - in: query
          name: joinDateTo
          schema:
            type: string
            format: date
        - in: query
          name: followersMin
          schema:
            type: integer
        - in: query
          name: followersMax
          schema:
            type: integer
        - in: query
          name: search
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
            default: 0
        - in: query
          name: size
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Creator list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminCreatorSummary'
                  page:
                    type: integer
                  size:
                    type: integer
                  total:
                    type: integer

  /admin/creators/{id}:
    get:
      tags: [Admin Creators]
      summary: Get creator detail for admin
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Creator detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCreatorDetail'

  /admin/creators/{id}/status:
    post:
      tags: [Admin Creators]
      summary: Update creator status
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminStatusUpdateRequest'
      responses:
        '200':
          description: Status updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCreatorSummary'

  /admin/creators/{id}/reputation/adjust:
    post:
      tags: [Admin Creators]
      summary: Adjust creator reputation
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminReputationAdjustRequest'
      responses:
        '200':
          description: Reputation updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCreatorSummary'

  /admin/creators/{id}/campaigns:
    get:
      tags: [Admin Creators]
      summary: List campaigns for a creator
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Campaigns for creator
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminCampaignSummary'

  /admin/creators/{id}/financial:
    get:
      tags: [Admin Creators]
      summary: Financial view for creator
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Financial summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreatorEarningsSummary'

  /admin/creators/{id}/reputation-history:
    get:
      tags: [Admin Creators]
      summary: Reputation history timeline
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Reputation history
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  properties:
                    timestamp:
                      type: string
                      format: date-time
                    score:
                      type: integer
                    reason:
                      type: string

  /admin/creators/{id}/audit-log:
    get:
      tags: [Admin Creators]
      summary: Audit log for creator actions
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Audit log entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminAuditLogEntry'

  /admin/reputation/overview:
    get:
      tags: [Admin Creators]
      summary: Reputation overview dashboard
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Reputation metrics
          content:
            application/json:
              schema:
                type: object
                properties:
                  averageScore:
                    type: number
                    format: double
                  tierCounts:
                    type: object
                    additionalProperties:
                      type: integer
                  manualAdjustments30d:
                    type: integer

  /admin/reputation/manual-adjustments:
    get:
      tags: [Admin Creators]
      summary: Recent manual reputation adjustments
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Adjustments list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminReputationAdjustRequest'

  /admin/reputation/tiers:
    put:
      tags: [Admin Creators]
      summary: Update reputation tier thresholds
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                sTier:
                  type: integer
                aTier:
                  type: integer
                bTier:
                  type: integer
                cTier:
                  type: integer
      responses:
        '200':
          description: Updated thresholds
          content:
            application/json:
              schema:
                type: object
                properties:
                  sTier:
                    type: integer
                  aTier:
                    type: integer
                  bTier:
                    type: integer
                  cTier:
                    type: integer

# ============ ADMIN PROJECTS ============
  /admin/projects:
    get:
      tags: [Admin Projects]
      summary: List projects with filters
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/AdminUserStatus'
        - in: query
          name: reputationMin
          schema:
            type: integer
        - in: query
          name: reputationMax
          schema:
            type: integer
        - in: query
          name: campaignCountMin
          schema:
            type: integer
        - in: query
          name: campaignCountMax
          schema:
            type: integer
        - in: query
          name: search
          schema:
            type: string
        - in: query
          name: page
          schema:
            type: integer
            default: 0
        - in: query
          name: size
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Project list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminProjectSummary'
                  page:
                    type: integer
                  size:
                    type: integer
                  total:
                    type: integer

  /admin/projects/{id}:
    get:
      tags: [Admin Projects]
      summary: Get project detail for admin
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Project detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminProjectDetail'

  /admin/projects/{id}/status:
    post:
      tags: [Admin Projects]
      summary: Update project status
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminStatusUpdateRequest'
      responses:
        '200':
          description: Status updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminProjectSummary'

  /admin/projects/{id}/reputation/adjust:
    post:
      tags: [Admin Projects]
      summary: Adjust project reputation
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminReputationAdjustRequest'
      responses:
        '200':
          description: Reputation updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminProjectSummary'

  /admin/projects/{id}/campaigns:
    get:
      tags: [Admin Projects]
      summary: List campaigns created by project
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Campaign list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminCampaignSummary'

  /admin/projects/{id}/financial:
    get:
      tags: [Admin Projects]
      summary: Financial view for project
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Financial summary
          content:
            application/json:
              schema:
                type: object
                properties:
                  escrowLocked:
                    type: number
                    format: double
                  transactionHistory:
                    type: array
                    items:
                      $ref: '#/components/schemas/ProjectTransaction'

  /admin/projects/{id}/audit-log:
    get:
      tags: [Admin Projects]
      summary: Audit log for project actions
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Audit log entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminAuditLogEntry'

# ============ ADMIN CAMPAIGNS ============
  /admin/campaigns:
    get:
      tags: [Admin Campaigns]
      summary: List campaigns for oversight
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/CampaignStatus'
        - in: query
          name: flagged
          schema:
            type: boolean
        - in: query
          name: cvpiMin
          schema:
            type: number
            format: double
        - in: query
          name: budgetMin
          schema:
            type: number
            format: double
        - in: query
          name: budgetMax
          schema:
            type: number
            format: double
        - in: query
          name: page
          schema:
            type: integer
            default: 0
        - in: query
          name: size
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Campaign list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminCampaignSummary'
                  page:
                    type: integer
                  size:
                    type: integer
                  total:
                    type: integer

  /admin/campaigns/{id}:
    get:
      tags: [Admin Campaigns]
      summary: Get campaign detail for admin
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Campaign detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCampaignSummary'

  /admin/campaigns/{id}/force-action:
    post:
      tags: [Admin Campaigns]
      summary: Execute administrative force action on a campaign
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminForceActionRequest'
      responses:
        '200':
          description: Action applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCampaignSummary'

  /admin/campaigns/{id}/flag:
    post:
      tags: [Admin Campaigns]
      summary: Flag a campaign
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Flagged campaign
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCampaignSummary'

# ============ ADMIN DISPUTES ============
  /admin/disputes:
    get:
      tags: [Admin Disputes]
      summary: List disputes with filters
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: status
          schema:
            $ref: '#/components/schemas/DisputeStatus'
        - in: query
          name: issueType
          schema:
            $ref: '#/components/schemas/DisputeIssueType'
        - in: query
          name: escalated
          schema:
            type: boolean
        - in: query
          name: page
          schema:
            type: integer
            default: 0
        - in: query
          name: size
          schema:
            type: integer
            default: 20
      responses:
        '200':
          description: Dispute list
          content:
            application/json:
              schema:
                type: object
                properties:
                  items:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminDisputeSummary'
                  page:
                    type: integer
                  size:
                    type: integer
                  total:
                    type: integer

  /admin/disputes/{id}:
    get:
      tags: [Admin Disputes]
      summary: Get dispute detail
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Dispute detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDisputeDetail'

  /admin/disputes/{id}/resolve:
    post:
      tags: [Admin Disputes]
      summary: Resolve a dispute with an outcome
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminDisputeResolutionRequest'
      responses:
        '200':
          description: Resolution recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDisputeDetail'

# ============ ADMIN FINANCE ============
  /admin/finance/revenue:
    get:
      tags: [Admin Finance]
      summary: Revenue dashboard data
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Revenue summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminRevenueSummary'

  /admin/finance/fees:
    get:
      tags: [Admin Finance]
      summary: Get fee configuration
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Fee configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  tiers:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminFeeTier'
                  multipliers:
                    $ref: '#/components/schemas/AdminFeeMultipliers'
    put:
      tags: [Admin Finance]
      summary: Update fee configuration
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tiers:
                  type: array
                  items:
                    $ref: '#/components/schemas/AdminFeeTier'
                multipliers:
                  $ref: '#/components/schemas/AdminFeeMultipliers'
      responses:
        '200':
          description: Updated configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  tiers:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminFeeTier'
                  multipliers:
                    $ref: '#/components/schemas/AdminFeeMultipliers'

# ============ ADMIN CVPI ============
  /admin/cvpi/algorithm:
    get:
      tags: [Admin CVPI]
      summary: Get CVPI algorithm configuration
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Algorithm config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCVPIAlgorithm'
    put:
      tags: [Admin CVPI]
      summary: Update CVPI algorithm configuration
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminCVPIAlgorithm'
      responses:
        '200':
          description: Updated configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCVPIAlgorithm'

  /admin/cvpi/benchmarks:
    get:
      tags: [Admin CVPI]
      summary: CVPI benchmark data
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Benchmark data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminCVPIBenchmark'

  /admin/cvpi/analytics:
    get:
      tags: [Admin CVPI]
      summary: CVPI analytics
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Analytics
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCVPIAnalytics'

# ============ ADMIN SYSTEM ============
  /admin/system/health:
    get:
      tags: [Admin System]
      summary: System health overview
      security:
        - bearerAuth: []
      responses:
        '200':
          description: System health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSystemHealth'

  /admin/system/pause:
    post:
      tags: [Admin System]
      summary: Emergency pause platform
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminPauseRequest'
      responses:
        '200':
          description: Pause initiated
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string

  /admin/system/resume:
    post:
      tags: [Admin System]
      summary: Resume platform operations
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminResumeRequest'
      responses:
        '200':
          description: Resume acknowledged
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  message:
                    type: string

  /admin/system/audit-logs:
    get:
      tags: [Admin System]
      summary: System-wide audit logs
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: actionType
          schema:
            type: string
        - in: query
          name: entityType
          schema:
            type: string
        - in: query
          name: adminUser
          schema:
            type: string
        - in: query
          name: dateFrom
          schema:
            type: string
            format: date-time
        - in: query
          name: dateTo
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Audit log entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminAuditLogEntry'

# ============ ADMIN SEARCH ============
  /admin/search:
    get:
      tags: [Admin Search]
      summary: Global admin search across entities
      security:
        - bearerAuth: []
      parameters:
        - in: query
          name: q
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Search results
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AdminSearchResult'
